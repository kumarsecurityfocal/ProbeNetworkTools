# ProbeOps Environment Template
# Copy this file to .env.backend, .env.probe, etc. and fill in the values.
# NEVER commit the actual .env files with real values to Git.

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/probeops

# API Security
SECRET_KEY=change_this_to_a_secure_random_string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings
# Use comma-separated list for multiple origins: http://localhost:5000,https://probeops.com
CORS_ORIGINS=*

# Network Probe Settings
PROBE_TIMEOUT=5  # seconds

# Production Settings
# Uncomment these in production environment only
# DOMAIN=probeops.com
# SSL_EMAIL=admin@probeops.com
# SSL_STAGING=false  # set to true for Let's Encrypt staging environment (testing)

# Scheduled Task Settings
# MAX_WORKERS=4
# SCHEDULER_INTERVAL=60  # seconds