# Database connection
DATABASE_URL=postgresql+psycopg2://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}

# JWT Authentication
SECRET_KEY=super-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings
CORS_ORIGINS=http://localhost,http://frontend,https://probeops.com,https://www.probeops.com

# Diagnostic tool settings
PROBE_TIMEOUT=5